cmake_minimum_required(VERSION 2.8)
project( writeVideo )
find_package( OpenCV REQUIRED )
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable( cv_writeVideo writeVideo.cpp )
target_link_libraries( cv_writeVideo ${OpenCV_LIBS} )
